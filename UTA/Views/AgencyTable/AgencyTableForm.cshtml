@model UTA.Models.Agency

@{
  ViewBag.Title = "Uredi Agenciju";
  Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 class="m-2">Uredi Agenciju</h2>

@using (Html.BeginForm("Save", "AgencyTable"))
{
  <div class="form-group">
    @Html.LabelFor(d => d.Name)
    @Html.TextBoxFor(d => d.Name, "", new { @class = "form-control" })
    @Html.ValidationMessageFor(d => d.Name)
  </div>
  <div class="form-group">
    @Html.LabelFor(d => d.Logo)
    @Html.TextBoxFor(d => d.Logo, "", new { @class = "form-control" })
    @Html.ValidationMessageFor(d => d.Logo)
  </div>
  @Html.HiddenFor(d => d.Id)
  @Html.AntiForgeryToken()
  <button type="submit" class="btn btn-primary">Sačuvaj</button>
}

@section scripts {
  @Scripts.Render("~/Bundles/jqueryval")
}